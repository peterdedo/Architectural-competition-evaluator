# Audit aplikace MHDSI Impact Study

**Datum:** 2026-04-04  
**Verze balíčku:** 1.0.0-rc.1 (`package.json`)  
**Next.js:** 14.2.35 · **React:** 18.3.1 · **TypeScript:** ~5.6  

---

## 1. Shrnutí

| Oblast | Stav |
|--------|------|
| `tsc --noEmit` | OK |
| `vitest run` | OK — 15 souborů, 88 testů |
| `next build` | OK — statické stránky |
| `next lint` | 2 varování (react-hooks/exhaustive-deps) |
| `npm audit` | 6 zranitelností (5 moderate, 1 high) — viz § 6 |

---

## 2. Routy (produkční build)

| Cesta | Typ | First Load JS (orientačně) |
|-------|-----|----------------------------|
| `/` | ○ static | ~96 kB |
| `/studio` | ○ static | ~187 kB |
| `/report` | ○ static | ~142 kB |
| `/report/print` | ○ static | ~141 kB |
| `/_not-found` | — | — |

---

## 3. Testovací pokrytí (Vitest)

- Výpočty: `employment`, `housing`, `economic`, `civic`, `run-pipeline`, `resolve-assumptions`
- Report: `m7-scenario-consolidation`, `build-report-m6-consistency`, `build-report-m8-completeness`, `build-report-snapshot.golden`
- Studio: `p1-pipeline-derive`, `wizard-schema`, `wizard-state-migration`, `warning-field-navigation`
- App: `routes-smoke`

**Poznámka:** `test:e2e` (Playwright) v tomto běhu auditu nebyl spuštěn — vyžaduje samostatní přípravu prohlížeče/CI.

---

## 4. ESLint

1. `src/features/studio/steps/wizard-step-4.tsx` — `useMemo`: zbytečné závislosti (`employmentRampYears`, `alignEmploymentRampToProjectHorizon`, `rampYearsGlobal`).
2. `src/features/studio/steps/wizard-step-5.tsx` — obdobně pro bydlení.

Doporučení: srovnat závislosti s reálně používanými hodnotami uvnitř `useMemo`, nebo zdůvodnit a potlačit pravidlo lokálně s komentářem.

---

## 5. Architektura (stručně)

- **App Router** (`src/app/`) — homepage, studio, report (+ print).
- **Doména / výpočty** — `src/lib/mhdsi/calculations/`.
- **Průvodce** — `src/features/studio/` (Zustand, kroky, mapování na pipeline).
- **Report** — snapshot jako jeden zdroj (`build-report-snapshot`, `ReportSnapshotRenderer`).
- **Trust / copy** — `src/content/trust-framing-cs.ts` (sjednocené formulace).

---

## 6. Bezpečnost závislostí (`npm audit`)

| Závažnost | Zdroj | Poznámka |
|-----------|-------|----------|
| Moderate | řetězec `esbuild` → `vite` → `vitest` | Riziko hlavně u **dev serveru** (Vite/Vitest), ne produkčního Next bundle. Oprava často vyžaduje major upgrade Vitestu. |
| High | `next` 9.5–15.5.13 | Advisory týkající se Image Optimizer, RSC, rewrites, cache — **ověřit upgrade na opravenou řadu Next** v rámci semver projektu (`npm audit` / release notes). |

**Nebylo spuštěno:** `npm audit fix --force` (může přinést breaking changes na Next 16 / Vitest 4).

---

## 7. Záloha vytvořená při tomto auditu

| Položka | Hodnota |
|---------|---------|
| **Soubor** | `c:\Users\User\Documents\Cursor\Metodika_dopadovych_studii_AUDIT_BACKUP_2026-04-04_15-47-06.zip` |
| **Velikost** | cca 8,4 MB |
| **Obsah** | Celý adresář projektu **kromě:** `node_modules`, `.next`, `out`, `playwright-report`, `test-results` |

Po rozbalení spusťte `npm install` a případně `npm run build`.

---

## 8. Doporučené další kroky (neblokující)

1. Naplánovat kontrolovaný upgrade **Next.js** po ověření kompatibility (audit high).
2. Zvážit upgrade **Vitest/Vite** nebo akceptovat dev-only riziko esbuild s vědomím prostředí.
3. Vyčistit ESLint varování ve `wizard-step-4` / `wizard-step-5`.
4. Občas spustit `npm run test:e2e` v CI nebo lokálně před releasem.

---

*Vygenerováno automatizovaným auditem (lint, typecheck, test, build, npm audit) a ruční kontrolou struktury projektu.*
